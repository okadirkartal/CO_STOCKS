@{
    ViewBag.Title = "Home";
}

<div class="jumbotron">
    <h1>STOCKS CENTER</h1>
        @if (Session[fields.CommonKeys.userGUID] != null)
        {
            <p class="lead">Welcome,you can add new stock,see your stock list</p>
        }
        else
        {
<p class="lead">Stock center,membership,login </p>
    }
    
</div>

 